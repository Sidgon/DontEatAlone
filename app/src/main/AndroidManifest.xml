<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ch.mse.dea.donteatalone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".UserProfileActivity"
            android:label="@string/title_activity_user_profile">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity
            android:name=".EditUserProfileActivity"
            android:label="@string/title_activity_edit_user_profile" />
        <activity android:name=".LoginActivity">

        </activity>
        <activity android:name=".RegisterWithEmailActivity" />
        <activity
            android:name=".GoingEventsListActivity"
            android:label="@string/going_events_list_activity_tile" />
    </application>

</manifest>